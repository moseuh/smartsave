specVersion: "v1beta"
serviceId: "save"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "save-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
